# WARNING: Replace placeholder values before applying.
# For production, use External Secrets Operator or GCP Secret Manager.
apiVersion: v1
kind: Secret
metadata:
  name: livekit-secrets
  namespace: call-center
type: Opaque
stringData:
  # Generate with: openssl rand -hex 24
  LIVEKIT_API_KEY: "REPLACE_ME"
  LIVEKIT_API_SECRET: "REPLACE_ME"
  LIVEKIT_URL: "wss://calls.hellojade.ai"
---
apiVersion: v1
kind: Secret
metadata:
  name: twilio-secrets
  namespace: call-center
type: Opaque
stringData:
  TWILIO_ACCOUNT_SID: "REPLACE_ME"
  TWILIO_AUTH_TOKEN: "REPLACE_ME"
